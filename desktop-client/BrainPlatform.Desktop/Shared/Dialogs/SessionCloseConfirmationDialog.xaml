<Window x:Class="BrainPlatform.Desktop.Shared.Dialogs.SessionCloseConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认关闭" Width="430" Height="235" MinWidth="430" MinHeight="235"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="#FFFFFF">
    <Grid Margin="28,24,28,22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Border Width="36" Height="36" CornerRadius="18" Background="#FEF2F2" Margin="0,0,12,0">
                <TextBlock Text="!" Foreground="#DC2626" FontSize="20" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="{Binding TitleText}" FontSize="18" FontWeight="SemiBold" Foreground="#0F172A" />
                <TextBlock Text="此操作将结束当前会话。" Margin="0,3,0,0" FontSize="12.5" Foreground="#64748B" />
            </StackPanel>
        </StackPanel>
        <TextBlock Grid.Row="1" Text="{Binding MessageText}" Margin="0,20,0,16" TextWrapping="Wrap"
                   FontSize="13.5" LineHeight="21" Foreground="#334155" VerticalAlignment="Top" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Width="92" Height="36" Margin="0,0,10,0" IsCancel="True" />
            <Button Content="{Binding ConfirmText}" Width="112" Height="36" Background="#DC2626" Foreground="#FFFFFF"
                    BorderBrush="#DC2626" IsDefault="True" Click="OnConfirmClick" />
        </StackPanel>
    </Grid>
</Window>
