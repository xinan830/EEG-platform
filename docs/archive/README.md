# Documentation Archive

Archived documents provide historical context only. They are not the source of truth for current behavior, scientific semantics, API contracts, or implementation guidance.

For current contracts, read `openspec/specs/`. For completed decision records, read `openspec/changes/archive/`.
