# 时频图 Canvas 渲染修复

## 问题

旧实现用大量 DOM 色块拼接矩阵，缺少稳定的功率动态范围和方向处理，造成块状噪声、性能开销和频率方向不直观。

## 修复

改为单个 Canvas 绘制；功率先转换为 dB，再使用 5%～98% 分位范围映射颜色；绘制时反转行索引，使 1 Hz 在底部、30 Hz 在顶部。保留用户选中的通道，不强制切换到 Fz。

## 验证

- `vue-tsc --noEmit` 通过。
- 前端 30 项测试通过。
- 未修改后端算法和 Viewer Pipeline。
