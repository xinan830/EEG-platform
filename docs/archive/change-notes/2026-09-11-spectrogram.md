# Spectrogram 第一版

新增时频图接口与前端热力图。算法固定使用连续 `offline-spectral-v3` 预处理后的信号；当前契约已统一为 4 秒 Hann 窗、1 秒步进、1–30 Hz，输出单位 `µV²/Hz`。前端只负责颜色映射和展示，不执行频谱计算。补充频率轴、时间轴和功率图例。

当前限制：时间/频率游标、色阶设置、波形点击联动和长窗口性能优化尚未实现；时频图属于工程分析视图，不是临床诊断结论。
