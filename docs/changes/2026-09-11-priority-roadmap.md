# EEG 阅图优先级落地状态

## 已完成

| 优先级 | 项目 | 状态 | 证据 |
| --- | --- | --- | --- |
| P0 | 固定滤波器契约 | 已完成 | `display-iir-sos-v2`；因果 SOS、显式基线开关；接口返回 `filter_contract` |
| P0 | 静态窗口与播放状态一致 | 已完成 | 从文件起点按 50 ms 块推进；定位后滤波预热 |
| P0 | 独立算法回归测试 | 已完成 | `backend/tests/test_filter_contract_regression.py` 黄金值测试 |
| P0 | 离线频谱算法契约 | 已完成（工程口径） | `offline-spectral-v2`；独立 SOS/PSD/指标/FAA 测试和输入参数快照 |
| P1 | 平均参考通道配置 | 已完成 | `average_exclude` 支持任意文件通道，默认 `AVG-All` |
| P1 | Montage 基础完整化 | 已完成 | 横向双极、Cz 参考、标准 16/18/20 通道方案 |
| P1 | raw / 显示 / 分析边界 | 已完成 | 原始文件只读；显示处理在副本上完成 |
| P2 | 全通道性能第一版 | 已完成 | 回放按块读取；静态窗口复用按文件、滤波参数和通道集合索引的滤波状态检查点 |
| P2 | 操作审计基础 | 部分完成 | SQLite 审计事件、参数快照和本地查询 API |
| P2 | 人工事件标记基础闭环 | 已完成（基础能力） | 后端持久化、绝对时间校验、查询/删除 API；前端时间轴创建、跳转和删除 |
| P2 | 报告快照基础能力 | 已完成（基础能力） | 独立快照实体、创建/列表/读取 API，记录快照元数据并写入审计 |

## 待实现

| 优先级 | 项目 | 当前边界 | 下一步 |
| --- | --- | --- | --- |
| P2 | 临床审计与报告 | 已有审计实体，但没有身份权限、防篡改、签名导出和报告版本 | 先完成身份与权限边界，再实现报告快照、版本和签名导出 |

## 验证基线

- 后端：53 个测试通过。
- 前端：`vue-tsc --noEmit`、22 个 Vitest 测试通过。
- 文件规模：生产代码未新增超过 400 行文件；`App.vue` 保持在 400 行以内。
- 性能边界：检查点缓存已有进程内 LRU 容量上限，服务重启后重建；尚未完成压力基准和跨进程共享。
