# 频谱加载触发控制

## 变更内容

频谱面板不再监听播放游标变化，避免连续播放时不断发起整段离线频谱计算。仅在 recording、通道列表变化时自动加载；用户可通过“刷新频谱”重新计算当前分析窗口。

## 影响

Viewer 播放链不变。PSD 曲线增加通道选择，功率数据继续完全来自后端，不在前端重算。

## 验证

- 前端 29 项测试通过。
- `vue-tsc --noEmit` 通过。
- 后端 78 项测试通过。
