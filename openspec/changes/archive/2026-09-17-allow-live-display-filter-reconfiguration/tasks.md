## 1. Contract and Implementation

- [x] Add optional bounded warm-up request and response fields to local live
  filter session creation.
- [x] Add a contiguous raw warm-up factory and versioned filter session switch.
- [x] Permit controls during recording and clear stale display history on a
  successful setting change.

## 2. Verification

- [x] Add backend warm-up/filter and desktop contiguous-tail regression tests.
- [x] Run focused backend tests, desktop tests, Release build, diff check, and
  strict OpenSpec validation.
