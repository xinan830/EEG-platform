## Design

A frontend-only label map converts known `platform-official` definition names
to Chinese display labels. Unknown and user definitions retain their returned
name. The database identity remains the backend value.
