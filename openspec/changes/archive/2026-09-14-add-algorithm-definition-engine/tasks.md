## 1. Definition persistence and contracts

- [x] 1.1 Add non-destructive schema migration, typed models, repository, SemVer and immutable lifecycle validation.
- [x] 1.2 Add canonical graph/config digest and constrained parameter-schema validation.

## 2. Graph validation and execution

- [x] 2.1 Implement closed graph parsing, topology sort, node input/kind/unit checks and structured failures.
- [x] 2.2 Implement restricted formula parser/evaluator with no `eval`, imports, attributes or arbitrary calls.
- [x] 2.3 Execute primitives with quality/provenance propagation and support explicit preview-only output.

## 3. API and verification

- [x] 3.1 Add Definition create, clone, validate, publish, list/get and version comparison API resources.
- [x] 3.2 Add migration, persistence, graph, formula, API and legacy-regression tests.
- [x] 3.3 Document contracts, complete strict validation, full gates, evidence report, spec sync and archive.
