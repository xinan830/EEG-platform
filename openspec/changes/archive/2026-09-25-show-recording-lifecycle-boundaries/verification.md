# Physical-device acceptance

On 2026-09-25, the user reported completing an ANT/eego recording with the
following sequence: connect, prepare, start, pause for several seconds, resume,
stop, save, and reopen in review. The user reported no issue with the device
identity, channel/sampling configuration, millisecond lifecycle labels,
acquisition and review boundary markers, explicit blank pause interval,
post-resume waveform position, event/time alignment, light review grid, or
recording reopen.

This is user-reported physical-device acceptance of the pause/resume workflow.
No recording ID, raw audit file, device log, or screenshots were provided for
independent inspection in this change. In particular, the precise persisted
sample-counter values and UTC timestamps at each boundary were not independently
reproduced here. This report does not establish external millisecond clock
synchronization or clinical validity.
